

class Thumbnail:

    def __init__(self, img_url, title, view_counts, published_at) -> None:
        self.img_url = img_url
        self.title = title
        self.view_counts = view_counts
        self.published_at = published_at